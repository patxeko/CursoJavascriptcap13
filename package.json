{
  "name": "07-http-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 5174"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^8.0.12"
  }
}
